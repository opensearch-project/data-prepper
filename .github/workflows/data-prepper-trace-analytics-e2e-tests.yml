# This workflow will build a Java project with Gradle
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: Data Prepper Trace Analytics End-to-end test with Gradle

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    strategy:
      matrix:
        java: [14]

    runs-on: ubuntu-latest

    steps:
    - name: Set up JDK ${{ matrix.java }}
      uses: actions/setup-java@v1
      with:
        java-version: ${{ matrix.java }}
    # TODO: replace local built OpenSearch artifact with the public artifact
    - name: Checkout OpenSearch
      uses: actions/checkout@v2
      with:
        repository: 'opensearch-project/OpenSearch'
        path: OpenSearch
        ref: '1.0'
    - name: Build OpenSearch
      working-directory: ./OpenSearch
      run: ./gradlew publishToMavenLocal -Dbuild.version_qualifier=rc1 -Dbuild.snapshot=false
    - name: Checkout Data-Prepper
      uses: actions/checkout@v2
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    # - name: Run raw-span end-to-end tests with Gradle    # TODO: re-enable once TraceGroupPrepper is added back
    #   run: ./gradlew :data-prepper-core:rawSpanEndToEndTest
    - name: Run service-map end-to-end tests with Gradle
      run: ./gradlew :data-prepper-core:serviceMapEndToEndTest
