logstash-converted-pipeline:
  source:
    http:
      max_connection_count: 500
      request_timeout: 10000
  processor:
    - grok:
        match:
          message:
            - "%{COMBINEDAPACHELOG}"
  sink:
    - opensearch:
        hosts:
          - "localhost:9200"
