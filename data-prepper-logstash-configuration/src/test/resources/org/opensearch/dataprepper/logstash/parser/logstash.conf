input {
    http {
        port => 8000
        threads => 100
        max_pending_requests => 128
    }
}
output {
    elasticsearch {
        hosts => ["https://localhost:9200"]
        user => admin
        password => admin
    }
}