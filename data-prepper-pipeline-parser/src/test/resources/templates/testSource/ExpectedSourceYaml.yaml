template-pipeline:
  source:
    documentdb:
      hostname: "database.example.com"
      port: 27017
  sink:
    opensearch:
      hosts: ["https://search-service.example.com"]
      index: "my_index"
      aws:
        sts_role_arn: "arn123"
        region: "us-test-1"
      dlq:
        s3:
          bucket: "test-bucket"
