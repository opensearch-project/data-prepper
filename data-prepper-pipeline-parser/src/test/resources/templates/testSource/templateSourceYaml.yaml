template-pipeline:
  source:
    documentdb:
      hostname: "{{dodb-pipeline.source.documentdb.hostname}}"
      port: "{{dodb-pipeline.source.documentdb.port}}"
  sink:
    opensearch:
      hosts: "{{dodb-pipeline.sink.opensearch.hosts}}"
      index: "{{dodb-pipeline.sink.opensearch.index}}"
      aws:
        sts_role_arn: "arn123"
        region: "us-test-1"
      dlq:
          s3:
              bucket: "test-bucket"
