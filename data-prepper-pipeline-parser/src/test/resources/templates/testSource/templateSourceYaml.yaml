template-pipeline:
  source:
    documentdb:
      hostname: "{{$..source.documentdb.hostname}}"
      port: "{{$...source.documentdb.port}}"
  sink:
    - opensearch:
       hosts: "{{$..sink.opensearch.hosts}}"
       index: "{{$..sink.opensearch.index}}"
       aws:
        sts_role_arn: "arn123"
        region: "us-test-1"
       dlq:
          s3:
              bucket: "test-bucket"
