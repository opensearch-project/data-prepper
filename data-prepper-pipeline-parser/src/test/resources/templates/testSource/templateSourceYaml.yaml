template-pipeline:
  source: "{{$..source}}"
  sink:
    - opensearch:
       hosts: "hosts"
       index: "index"
       aws:
        sts_role_arn: "arn123"
        region: "us-test-1"
       dlq:
          s3:
              bucket: "test-bucket"
