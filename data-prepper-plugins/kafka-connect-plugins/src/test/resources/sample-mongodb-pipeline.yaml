log-pipeline:
  source:
    mongodb:
      hostname: localhost
      credentials:
        plaintext:
          username: debezium
          password: dbz
      collections:
        - topic_prefix: prefix1
          collection_name: test.customers
  sink:
    - noop: