log-pipeline:
  source:
    mysql:
      hostname: localhost
      credentials:
        plaintext:
          username: debezium
          password: dbz
      tables:
        - topic_prefix: prefix1
          table_name: inventory.customers
  sink:
    - noop: