apply from: file("build-resources.gradle")

allprojects {
    dependencies {
        compile project(':data-prepper-core')
    }
}

task endToEndTests {
    //dependsOn(':data-prepper-core:rawSpanEndToEndTest')
}

task benchmarkTests {
    //dependsOn(':situp-api:test') TODO add benchmark test and enable
}

task buildCore {
    dependsOn ':data-prepper-core:build'
}

task releasePrerequisites {
    dependsOn 'buildCore'
    dependsOn 'endToEndTests'
    dependsOn 'benchmarkTests'
}