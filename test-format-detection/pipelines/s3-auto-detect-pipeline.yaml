# Auto-detect pipeline: no codec specified — format detection picks the codec
s3-auto-detect-pipeline:
  source:
    s3:
      aws:
        region: "us-east-1"
        sts_role_arn: "arn:aws:iam::463470984675:role/Admin"
      scan:
        buckets:
          - bucket:
              name: "siqi-format-detector-bucket"
              filter:
                include_prefix:
                  - "test-data/"
  sink:
    - stdout:
